version: "3.8"
services:
  web:
    platform: "linux/amd64"
    build:
      context: .
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - gem_cache:/usr/local/bundle/gems
    environment:
      RAILS_ENV: development
      DATABASE_URL: "postgresql://postgres:postgres@database/nwspk_development"
      REDIS_URL: "redis://redis:6379/0"
      SECRET_KEY_BASE: "xxx"
      MAILGUN_SMTP_LOGIN: "896e1902-98fc-4176-8266-43cba5d6f2d3"
      MAILGUN_SMTP_PASSWORD: "896e1902-98fc-4176-8266-43cba5d6f2d3"
      MAILGUN_SMTP_PORT: "587"
      MAILGUN_SMTP_SERVER: "smtp.postmarkapp.com"
      SEND_REMINDERS: 1
      STRIPE_KEY: "sk_xxx"
      STRIPE_PUBLIC_KEY: "pk_xxx"
      STRIPE_TAX_RATE_ID: "txr_xxx"

  database:
    image: postgres:14
    environment:
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: "pg_isready -U postgres"
      interval: 5s
      timeout: 10s
      retries: 120
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    platform: "linux/amd64"
    image: redis:5.0

  sidekiq:
    platform: "linux/amd64"
    build:
      context: .
    depends_on:
      - web
      - database
      - redis
    volumes:
      - .:/app
      - gem_cache:/usr/local/bundle/gems
    environment:
      RAILS_ENV: development
      REDIS_URL: "redis://redis:6379/0"
    entrypoint: ./sidekiq-entrypoint.sh

volumes:
  gem_cache:
  postgres_data:
