Hello <%= @user.name %>,


Upcoming events
===============

<% @upcoming_events.each do |e| %>
<%= e.start_at.strftime('%e %A %B, %Y') %>
<%= [e.start_at.strftime('%H:%M'), e.end_at.strftime('%H:%M')].join(' - ') %> in <%= e.location %>: <%= e.name %>
<%= e.short_description %>

<% end %>

For more details, see: <%= events_url %>
Subscribe with Apple Calendar: <%= api_events_url %>
Subscribe with Google Calendar: <%= "https://calendar.google.com/calendar/embed?src=#{ENV['GOOGLE_CALENDAR_ID']}" %>

New Members This Month
======================

<% @new_users.each do |u| %>
• <%= plaintext_list_user_item(u) %>
<% end %>

See all members: <%= graphs_full_url %>

Current Fellows
======================

<% @fellows.each do |u| %>
• <%= plaintext_list_user_item(u) %>
<% end %>

See all fellows: <%= fellowship_path %>

Billing Invoice
===============

<%= @ascii_table %>

You will be billed with your card ending in <%= @card_num %> (change payment details: <%= edit_subscription_url %>)


____

Any problems with your membership, or if you see anything funny with this email, please contact ed@nwspk.com

Newspeak House
