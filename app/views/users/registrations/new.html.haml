- content_for :page_title do
  Membership

%p.lead.space Support Newspeak House with Membership

%p Help Newspeak House stay independent, and join a network of people working to improve politics with technology.

%p Memberships are £25 per month, charged by monthly subscription, with a one-off joining fee of £50.

- unless user_signed_in?
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal' }, wrapper: :horizontal_form, wrapper_mappings: { radio_buttons: :horizontal_radio_and_checkboxes }) do |f|
    = f.error_notification

    = f.input :name, required: true
    = f.input :email, required: true
    = f.input :url, wrapper_html: { style: 'display: none' }
    = f.input :password, required: true, hint: ("(min #{@minimum_password_length} characters)" if @validatable)
    = f.input :password_confirmation, required: true

    %p You can also sponsor memberships for others:

    = f.input :sponsor, collection: Plan.visible
    = f.input :applicant, as: :boolean, label: 'Apply for a sponsored membership'

    %p Tell us briefly why you'd like to join Newspeak House - please include details of past & current projects, activism, organisational affiliations, etc

    = f.input :application_text

    .form-group.form-submit
      .col-6
        = f.button :submit, 'Submit', class: 'btn-success'
