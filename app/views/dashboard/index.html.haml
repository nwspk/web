- content_for :page_title do
  Account

%div{:style => 'margin-bottom: 1em;'}
  = link_to 'Change password', edit_user_registration_path
  - if can? :manage, :all
    •
    = link_to 'Admin UI', admin_root_path
  •
  = link_to 'Logout', destroy_user_session_path, method: :delete

%table
  %tbody
    -# %tr
    -#   %th Name
    -#   %td= best_in_place current_user, :name, as: :input, url: user_path(format: 'json'), display_with: lambda { |v| "#{h(v)} ✎".html_safe }
    -# %tr
    -#   %th Bio
    -#   %td= best_in_place current_user, :showcase_text, as: :input, url: user_path(format: 'json'), display_with: lambda { |v| "#{h(v)} ✎".html_safe }
    -# %tr
    -#   %th Email
    -#   %td= best_in_place current_user, :email, as: :input, url: user_path(format: 'json'), display_with: lambda { |v| "#{h(v)} ✎".html_safe }
    -# %tr
    -#   %th Website
    -#   %td= best_in_place current_user, :url, as: :input, url: user_path(format: 'json'), display_with: lambda { |v| "#{h(v)} ✎".html_safe }
    %tr
      %th Subscription
      - if !current_user.subscription.nil? && current_user.subscription.active?
        %td
          = current_user.subscription.plan.name
          = link_to 'Change plan', edit_subscription_path
      - elsif current_user.subscription.nil?
        %td
          None
      - else
        %td
          Inactive
          (#{link_to('change', edit_subscription_path)})
      -# %tr
      -#   %th Avatar
      -#   %td
      -#     = image_tag current_user.avatar_url, width: 80, height: 80
      -#     = simple_form_for(current_user, url: user_path) do |f|
      -#       = f.input :avatar, as: :file, label: false
      -#       = f.submit 'Upload'
